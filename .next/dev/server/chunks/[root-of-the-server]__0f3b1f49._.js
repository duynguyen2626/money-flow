module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[project]/src/lib/supabase/server.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createClient",
    ()=>createClient
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f40$supabase$2b$ssr$40$0$2e$7$2e$0_$40$supabase$2b$supabase$2d$js$40$2$2e$89$2e$0$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/@supabase+ssr@0.7.0_@supabase+supabase-js@2.89.0/node_modules/@supabase/ssr/dist/module/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f40$supabase$2b$ssr$40$0$2e$7$2e$0_$40$supabase$2b$supabase$2d$js$40$2$2e$89$2e$0$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/@supabase+ssr@0.7.0_@supabase+supabase-js@2.89.0/node_modules/@supabase/ssr/dist/module/createServerClient.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$0$2e$10_$40$babel$2b$core$40$7$2e$28$2e$5_react$2d$dom$40$19$2e$2$2e$3_react$40$19$2e$2$2e$3_$5f$react$40$19$2e$2$2e$3$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/next@16.0.10_@babel+core@7.28.5_react-dom@19.2.3_react@19.2.3__react@19.2.3/node_modules/next/headers.js [app-route] (ecmascript)");
;
;
function createClient() {
    const cookieStore = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$0$2e$10_$40$babel$2b$core$40$7$2e$28$2e$5_react$2d$dom$40$19$2e$2$2e$3_react$40$19$2e$2$2e$3_$5f$react$40$19$2e$2$2e$3$2f$node_modules$2f$next$2f$headers$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["cookies"])();
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f40$supabase$2b$ssr$40$0$2e$7$2e$0_$40$supabase$2b$supabase$2d$js$40$2$2e$89$2e$0$2f$node_modules$2f40$supabase$2f$ssr$2f$dist$2f$module$2f$createServerClient$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createServerClient"])(("TURBOPACK compile-time value", "https://puzvrlojtgneihgvevcx.supabase.co"), ("TURBOPACK compile-time value", "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InB1enZybG9qdGduZWloZ3ZldmN4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM1NDI5NTksImV4cCI6MjA3OTExODk1OX0.fAVI34PhJBDxN8iZU6Eb_EPfE5YKJ9sg-oDI0LzlU4w"), {
        cookies: {
            async get (name) {
                const store = await cookieStore;
                return store.get(name)?.value;
            },
            async set (name, value, options) {
                try {
                    const store = await cookieStore;
                    store.set({
                        name,
                        value,
                        ...options
                    });
                } catch  {
                // The `set` method was called from a Server Component.
                // This can be ignored if you have middleware refreshing sessions.
                }
            },
            async remove (name, options) {
                try {
                    const store = await cookieStore;
                    store.set({
                        name,
                        value: '',
                        ...options
                    });
                } catch  {
                // The `delete` method was called from a Server Component.
                // This can be ignored if you have middleware refreshing sessions.
                }
            }
        }
    });
}
}),
"[project]/src/services/pocketbase/server.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "generatePocketBaseId",
    ()=>generatePocketBaseId,
    "pocketbaseGetById",
    ()=>pocketbaseGetById,
    "pocketbaseList",
    ()=>pocketbaseList,
    "pocketbaseRequest",
    ()=>pocketbaseRequest,
    "toPocketBaseId",
    ()=>toPocketBaseId
]);
const PB_URL = 'https://api-db.reiwarden.io.vn';
let cachedPocketBaseToken = null;
async function getPocketBaseAuthHeaders() {
    const explicitToken = process.env.POCKETBASE_ADMIN_TOKEN || process.env.POCKETBASE_SERVICE_TOKEN || process.env.POCKETBASE_TOKEN || process.env.PB_ADMIN_TOKEN || process.env.PB_SERVICE_TOKEN || '';
    if (explicitToken) {
        return {
            Authorization: `Bearer ${explicitToken}`
        };
    }
    if (cachedPocketBaseToken) {
        return {
            Authorization: `Bearer ${cachedPocketBaseToken}`
        };
    }
    const identity = process.env.POCKETBASE_DB_EMAIL || '';
    const password = process.env.POCKETBASE_DB_PASSWORD || '';
    if (!identity || !password) {
        return {};
    }
    const authEndpoints = [
        '/api/admins/auth-with-password',
        '/api/collections/_superusers/auth-with-password'
    ];
    for (const endpoint of authEndpoints){
        try {
            const response = await fetch(`${PB_URL}${endpoint}`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    identity,
                    password
                })
            });
            if (!response.ok) continue;
            const payload = await response.json();
            if (payload?.token) {
                cachedPocketBaseToken = payload.token;
                return {
                    Authorization: `Bearer ${payload.token}`
                };
            }
        } catch  {
        // try next endpoint
        }
    }
    return {};
}
function toQueryString(params) {
    if (!params) return '';
    const searchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(params)){
        if (value !== undefined && value !== null) {
            searchParams.set(key, String(value));
        }
    }
    const query = searchParams.toString();
    return query ? `?${query}` : '';
}
function normalizeBody(body) {
    if (body === undefined || body === null) return undefined;
    if (typeof body === 'string' || body instanceof URLSearchParams || body instanceof FormData || body instanceof Blob || body instanceof ArrayBuffer || ArrayBuffer.isView(body)) {
        return body;
    }
    return JSON.stringify(body);
}
async function pocketbaseRequest(endpoint, options) {
    const url = `${PB_URL}${endpoint}`;
    const authHeaders = await getPocketBaseAuthHeaders();
    const response = await fetch(url, {
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...authHeaders,
            ...options?.headers ?? {}
        },
        body: normalizeBody(options?.body)
    });
    if (!response.ok) {
        const text = await response.text();
        throw new Error(`PB request failed [${response.status}]: ${text}`);
    }
    if (response.status === 204) {
        return undefined;
    }
    return await response.json();
}
async function pocketbaseList(collection, params) {
    const query = toQueryString(params);
    return pocketbaseRequest(`/api/collections/${collection}/records${query}`);
}
async function pocketbaseGetById(collection, id) {
    return pocketbaseRequest(`/api/collections/${collection}/records/${id}`);
}
function toPocketBaseId(supabaseId, _collection) {
    if (!supabaseId) return generatePocketBaseId();
    const hash = Array.from(supabaseId).reduce((acc, ch)=>(acc << 5) - acc + ch.charCodeAt(0), 0).toString(36).replace('-', 'n').slice(0, 15).padEnd(15, '0');
    return hash;
}
function generatePocketBaseId() {
    const chars = 'abcdefghijklmnopqrstuvwxyz0123456789';
    let id = '';
    for(let i = 0; i < 15; i++){
        id += chars.charAt(Math.floor(Math.random() * chars.length));
    }
    return id;
}
}),
"[project]/src/app/api/migrate/backfill/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * One-time PocketBase backfill / re-sync route.
 * Usage:
 *   GET /api/migrate/backfill?collection=accounts
 *   GET /api/migrate/backfill?collection=transactions
 *   GET /api/migrate/backfill?collection=all          (accounts first, then transactions)
 *
 * Each run is fully idempotent — existing PB records are PATCHed, missing ones are POSTed.
 */ __turbopack_context__.s([
    "GET",
    ()=>GET
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$0$2e$10_$40$babel$2b$core$40$7$2e$28$2e$5_react$2d$dom$40$19$2e$2$2e$3_react$40$19$2e$2$2e$3_$5f$react$40$19$2e$2$2e$3$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/.pnpm/next@16.0.10_@babel+core@7.28.5_react-dom@19.2.3_react@19.2.3__react@19.2.3/node_modules/next/server.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/supabase/server.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/services/pocketbase/server.ts [app-route] (ecmascript)");
;
;
;
const PB_TRANSACTIONS_COLLECTION = 'pvl_txn_001';
const PB_ACCOUNTS_COLLECTION = 'pvl_acc_001';
const PB_CATEGORIES_COLLECTION = 'pvl_cat_001';
const PB_SHOPS_COLLECTION = 'pvl_shop_001';
const PB_PEOPLE_COLLECTION = 'pvl_people_001';
// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------
/** PATCH an existing PB record; fall back to POST if not found (404). */ async function upsertPB(collection, pbId, body) {
    try {
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${collection}/records/${pbId}`, {
            method: 'PATCH',
            body
        });
        return 'updated';
    } catch (err) {
        if (String(err?.message).includes('[404]')) {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${collection}/records`, {
                method: 'POST',
                body: {
                    id: pbId,
                    ...body
                }
            });
            return 'created';
        }
        throw err;
    }
}
/** Run tasks in parallel batches to avoid hammering PB while still being fast. */ async function runBatched(items, batchSize, fn) {
    for(let i = 0; i < items.length; i += batchSize){
        await Promise.allSettled(items.slice(i, i + batchSize).map(fn));
    }
}
function normalizeText(value) {
    return String(value ?? '').trim().toLowerCase();
}
function isPocketBaseId(value) {
    return Boolean(value && value.length === 15);
}
function pickUnique(candidates) {
    const uniq = Array.from(new Set(candidates.filter(Boolean)));
    return uniq.length === 1 ? uniq[0] : null;
}
function toIso(input) {
    if (!input) return '';
    const parsed = new Date(input);
    if (Number.isNaN(parsed.getTime())) return String(input).trim();
    return parsed.toISOString();
}
function normalizeNote(input) {
    return String(input ?? '').trim().toLowerCase().replace(/\s+/g, ' ');
}
function buildTxnFingerprint(input) {
    return [
        toIso(input.occurredAt),
        Math.round(Number(input.amount ?? 0)),
        normalizeText(input.type),
        normalizeNote(input.note),
        normalizeText(input.persistedCycleTag)
    ].join('|');
}
async function loadPocketBaseLookupRows(collection, fields) {
    const rows = [];
    let page = 1;
    while(true){
        const response = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${collection}/records?page=${page}&perPage=500&fields=${encodeURIComponent(fields)}`, {
            method: 'GET'
        });
        const items = response.items ?? [];
        rows.push(...items);
        const totalPages = response.totalPages ?? 1;
        if (page >= totalPages || items.length === 0) break;
        page += 1;
    }
    return rows;
}
async function purgeCollectionRecords(collection) {
    const errors = [];
    let deleted = 0;
    let failed = 0;
    const rows = await loadPocketBaseLookupRows(collection, 'id');
    await runBatched(rows, 20, async (row)=>{
        try {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${collection}/records/${row.id}`, {
                method: 'DELETE'
            });
            deleted += 1;
        } catch (err) {
            failed += 1;
            errors.push(`[purge:${collection}:${row.id}] ${String(err)}`);
        }
    });
    return {
        deleted,
        failed,
        errors
    };
}
async function buildIdBridgeMaps() {
    const supabase = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    const [supabaseAccountsResult, supabaseCategoriesResult, supabaseShopsResult, supabasePeopleResult, pbAccounts, pbCategories, pbShops, pbPeople] = await Promise.all([
        supabase.from('accounts').select('id,name,account_number'),
        supabase.from('categories').select('id,name,type'),
        supabase.from('shops').select('id,name'),
        supabase.from('people').select('id,name'),
        loadPocketBaseLookupRows(PB_ACCOUNTS_COLLECTION, 'id,name,slug,account_number'),
        loadPocketBaseLookupRows(PB_CATEGORIES_COLLECTION, 'id,name,type'),
        loadPocketBaseLookupRows(PB_SHOPS_COLLECTION, 'id,name'),
        loadPocketBaseLookupRows(PB_PEOPLE_COLLECTION, 'id,name')
    ]);
    if (supabaseAccountsResult.error) throw new Error(`accounts map fetch failed: ${supabaseAccountsResult.error.message}`);
    if (supabaseCategoriesResult.error) throw new Error(`categories map fetch failed: ${supabaseCategoriesResult.error.message}`);
    if (supabaseShopsResult.error) throw new Error(`shops map fetch failed: ${supabaseShopsResult.error.message}`);
    if (supabasePeopleResult.error) throw new Error(`people map fetch failed: ${supabasePeopleResult.error.message}`);
    const accountMap = new Map();
    const categoryMap = new Map();
    const shopMap = new Map();
    const personMap = new Map();
    const pbAccountById = new Set(pbAccounts.map((row)=>row.id));
    const pbAccountBySlug = new Map();
    const pbAccountByNumber = new Map();
    const pbAccountByName = new Map();
    for (const row of pbAccounts){
        const slug = normalizeText(row.slug);
        if (slug) pbAccountBySlug.set(slug, [
            ...pbAccountBySlug.get(slug) ?? [],
            row.id
        ]);
        const number = normalizeText(row.account_number);
        if (number) pbAccountByNumber.set(number, [
            ...pbAccountByNumber.get(number) ?? [],
            row.id
        ]);
        const name = normalizeText(row.name);
        if (name) pbAccountByName.set(name, [
            ...pbAccountByName.get(name) ?? [],
            row.id
        ]);
    }
    for (const account of supabaseAccountsResult.data ?? []){
        if (isPocketBaseId(account.id) && pbAccountById.has(account.id)) {
            accountMap.set(account.id, account.id);
            continue;
        }
        const slugPrefix = normalizeText(account.id.slice(0, 8));
        const number = normalizeText(account.account_number);
        const name = normalizeText(account.name);
        const match = pickUnique(pbAccountBySlug.get(slugPrefix) ?? []) || pickUnique(pbAccountByNumber.get(number) ?? []) || pickUnique(pbAccountByName.get(name) ?? []);
        if (match) accountMap.set(account.id, match);
    }
    function buildNameTypeMap(rows, includeType = false) {
        const map = new Map();
        for (const row of rows){
            const name = normalizeText(row.name);
            if (!name) continue;
            const key = includeType ? `${name}::${normalizeText(row.type)}` : name;
            map.set(key, [
                ...map.get(key) ?? [],
                row.id
            ]);
        }
        return map;
    }
    const pbCategoryByNameType = buildNameTypeMap(pbCategories, true);
    const pbCategoryById = new Set(pbCategories.map((row)=>row.id));
    for (const category of supabaseCategoriesResult.data ?? []){
        if (isPocketBaseId(category.id) && pbCategoryById.has(category.id)) {
            categoryMap.set(category.id, category.id);
            continue;
        }
        const key = `${normalizeText(category.name)}::${normalizeText(category.type)}`;
        const match = pickUnique(pbCategoryByNameType.get(key) ?? []);
        if (match) categoryMap.set(category.id, match);
    }
    const pbShopByName = buildNameTypeMap(pbShops);
    const pbShopById = new Set(pbShops.map((row)=>row.id));
    for (const shop of supabaseShopsResult.data ?? []){
        if (isPocketBaseId(shop.id) && pbShopById.has(shop.id)) {
            shopMap.set(shop.id, shop.id);
            continue;
        }
        const key = normalizeText(shop.name);
        const match = pickUnique(pbShopByName.get(key) ?? []);
        if (match) shopMap.set(shop.id, match);
    }
    const pbPersonByName = buildNameTypeMap(pbPeople);
    const pbPersonById = new Set(pbPeople.map((row)=>row.id));
    for (const person of supabasePeopleResult.data ?? []){
        if (isPocketBaseId(person.id) && pbPersonById.has(person.id)) {
            personMap.set(person.id, person.id);
            continue;
        }
        const key = normalizeText(person.name);
        const match = pickUnique(pbPersonByName.get(key) ?? []);
        if (match) personMap.set(person.id, match);
    }
    return {
        accountMap,
        categoryMap,
        shopMap,
        personMap
    };
}
// ---------------------------------------------------------------------------
// Accounts backfill
// ---------------------------------------------------------------------------
async function backfillAccounts() {
    const supabase = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    // Fetch all accounts; nulls-first so parent accounts are processed before children
    const { data: accountsRaw, error } = await supabase.from('accounts').select('*').order('parent_account_id', {
        ascending: true,
        nullsFirst: true
    });
    const accounts = accountsRaw ?? [];
    if (error) {
        return {
            created: 0,
            updated: 0,
            failed: 1,
            errors: [
                `fetch: ${error?.message}`
            ]
        };
    }
    let created = 0;
    let updated = 0;
    let failed = 0;
    const errors = [];
    // Two passes: first without FK references (so parent accounts land before children),
    // then a second pass to set FK fields like parent_account_id.
    const withFKs = [];
    // Pass 1 — create/update without optional FK fields
    await runBatched(accounts, 10, async (account)=>{
        const pbId = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(account.id);
        try {
            const body = {
                slug: account.id,
                name: account.name,
                // SB stores 'ewallet'; PB schema uses 'e_wallet'
                type: account.type === 'ewallet' ? 'e_wallet' : account.type ?? 'bank',
                currency: account.currency ?? 'VND',
                current_balance: account.current_balance ?? 0,
                credit_limit: account.credit_limit ?? 0,
                is_active: account.is_active ?? true,
                image_url: account.image_url ?? null,
                receiver_name: account.receiver_name ?? null,
                total_in: account.total_in ?? 0,
                total_out: account.total_out ?? 0,
                cashback_config: account.cashback_config ?? null,
                cashback_config_version: account.cashback_config_version ?? 1,
                annual_fee: account.annual_fee ?? null,
                annual_fee_waiver_target: account.annual_fee_waiver_target ?? null,
                cb_type: account.cb_type ?? 'none',
                cb_base_rate: account.cb_base_rate ?? 0,
                cb_max_budget: account.cb_max_budget ?? null,
                cb_is_unlimited: account.cb_is_unlimited ?? false,
                cb_rules_json: account.cb_rules_json ?? null,
                cb_min_spend: account.cb_min_spend ?? null,
                cb_cycle_type: account.cb_cycle_type ?? 'calendar_month',
                statement_day: account.statement_day ?? null,
                due_date: account.due_date ?? null,
                holder_type: account.holder_type ?? 'me',
                account_number: account.account_number ?? null
            };
            // Collect FK fields for pass 2
            const fkBody = {};
            if (account.owner_id) fkBody.owner_id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(account.owner_id);
            if (account.parent_account_id) fkBody.parent_account_id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(account.parent_account_id);
            if (account.secured_by_account_id) fkBody.secured_by_account_id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(account.secured_by_account_id);
            if (account.holder_person_id) fkBody.holder_person_id = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(account.holder_person_id);
            if (Object.keys(fkBody).length > 0) withFKs.push({
                pbId,
                fkBody
            });
            const result = await upsertPB('accounts', pbId, body);
            if (result === 'created') created++;
            else updated++;
        } catch (err) {
            failed++;
            errors.push(`[accounts:${account.id}] ${String(err)}`);
        }
    });
    // Pass 2 — patch FK references now that all accounts exist in PB
    await runBatched(withFKs, 10, async ({ pbId, fkBody })=>{
        try {
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/accounts/records/${pbId}`, {
                method: 'PATCH',
                body: fkBody
            });
        } catch (err) {
            errors.push(`[accounts FK pass pbId=${pbId}] ${String(err)}`);
        }
    });
    return {
        created,
        updated,
        failed,
        errors
    };
}
// ---------------------------------------------------------------------------
// Transactions backfill
// ---------------------------------------------------------------------------
async function backfillTransactions() {
    const supabase = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    const PAGE_SIZE = 500;
    let created = 0;
    let updated = 0;
    let failed = 0;
    const errors = [];
    const { accountMap, categoryMap, shopMap, personMap } = await buildIdBridgeMaps();
    // Build a map of existing PB records by source_id to avoid duplicates
    const pbRecordsBySourceId = new Map();
    try {
        let page = 1;
        while(true){
            const pbResp = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${PB_TRANSACTIONS_COLLECTION}/records?page=${page}&perPage=500&fields=id,metadata`);
            const items = pbResp?.items ?? [];
            items.forEach((item)=>{
                const sourceId = item.metadata?.source_id;
                if (sourceId) pbRecordsBySourceId.set(sourceId, item.id);
            });
            if (items.length < 500 || !pbResp?.totalPages || page >= pbResp.totalPages) break;
            page++;
        }
    } catch (err) {
        errors.push(`Failed to fetch existing PB records: ${String(err)}`);
    }
    let page = 0;
    while(true){
        const { data: transactionsRaw, error } = await supabase.from('transactions').select('id, occurred_at, note, type, account_id, target_account_id, category_id, person_id, shop_id, amount, status, tag, persisted_cycle_tag, cashback_share_percent, cashback_share_fixed, cashback_mode, metadata').order('occurred_at', {
            ascending: true
        }).range(page * PAGE_SIZE, (page + 1) * PAGE_SIZE - 1);
        const transactions = transactionsRaw ?? [];
        if (error) {
            errors.push(`page ${page}: ${error.message}`);
            break;
        }
        if (transactions.length === 0) break;
        await runBatched(transactions, 10, async (txn)=>{
            // Check if this source_id already exists in PB; if so, update that record instead of creating new
            const existingPbId = pbRecordsBySourceId.get(txn.id);
            const pbId = existingPbId ?? (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["toPocketBaseId"])(txn.id);
            try {
                const existingMeta = txn.metadata && typeof txn.metadata === 'object' ? txn.metadata : {};
                const mappedAccountId = accountMap.get(txn.account_id) ?? '';
                const mappedToAccountId = txn.target_account_id ? accountMap.get(txn.target_account_id) ?? '' : '';
                const mappedCategoryId = txn.category_id ? categoryMap.get(txn.category_id) ?? '' : '';
                const mappedPersonId = txn.person_id ? personMap.get(txn.person_id) ?? '' : '';
                const mappedShopId = txn.shop_id ? shopMap.get(txn.shop_id) ?? '' : '';
                const body = {
                    date: txn.occurred_at,
                    description: txn.note ?? '',
                    type: txn.type,
                    account_id: mappedAccountId,
                    to_account_id: mappedToAccountId,
                    category_id: mappedCategoryId,
                    person_id: mappedPersonId,
                    shop_id: mappedShopId,
                    amount: txn.amount,
                    final_price: txn.amount,
                    cashback_amount: 0,
                    is_installment: false,
                    parent_transaction_id: '',
                    metadata: {
                        ...existingMeta,
                        source_id: txn.id,
                        source_category_id: txn.category_id ?? null,
                        status: txn.status ?? 'posted',
                        tag: txn.tag ?? null,
                        persisted_cycle_tag: txn.persisted_cycle_tag ?? null,
                        cashback_share_percent: txn.cashback_share_percent ?? null,
                        cashback_share_fixed: txn.cashback_share_fixed ?? null,
                        cashback_mode: txn.cashback_mode ?? null
                    }
                };
                const result = await upsertPB(PB_TRANSACTIONS_COLLECTION, pbId, body);
                if (result === 'created') created++;
                else updated++;
            } catch (err) {
                failed++;
                errors.push(`[transactions:${txn.id}] ${String(err)}`);
            }
        });
        page++;
        if (transactions.length < PAGE_SIZE) break;
    }
    return {
        created,
        updated,
        failed,
        errors
    };
}
async function cleanupLegacyTransactions() {
    const supabase = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$supabase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createClient"])();
    let created = 0;
    let updated = 0;
    let failed = 0;
    const errors = [];
    const { accountMap, categoryMap, shopMap, personMap } = await buildIdBridgeMaps();
    const { data: sbRows, error: sbError } = await supabase.from('transactions').select('id, occurred_at, note, type, amount, account_id, target_account_id, category_id, person_id, shop_id, persisted_cycle_tag');
    if (sbError) {
        return {
            created,
            updated,
            failed: failed + 1,
            errors: [
                `cleanup supabase fetch failed: ${sbError.message}`
            ]
        };
    }
    const sbFingerprintMap = new Map();
    for (const row of sbRows ?? []){
        const key = buildTxnFingerprint({
            occurredAt: row.occurred_at,
            amount: row.amount,
            type: row.type,
            note: row.note,
            persistedCycleTag: row.persisted_cycle_tag
        });
        const list = sbFingerprintMap.get(key) ?? [];
        list.push(row);
        sbFingerprintMap.set(key, list);
    }
    const legacyPbRows = [];
    let page = 1;
    while(true){
        const response = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${PB_TRANSACTIONS_COLLECTION}/records?page=${page}&perPage=500&sort=-date`, {
            method: 'GET'
        });
        const items = response.items ?? [];
        legacyPbRows.push(...items);
        const totalPages = response.totalPages ?? 1;
        if (page >= totalPages || items.length === 0) break;
        page += 1;
    }
    const candidates = legacyPbRows.filter((row)=>{
        const metadata = row.metadata ?? {};
        const hasSource = typeof metadata.source_id === 'string' && metadata.source_id.length > 0;
        const hasMissingRelation = !row.account_id || !row.category_id;
        return !hasSource && hasMissingRelation;
    });
    await runBatched(candidates, 10, async (legacyRow)=>{
        try {
            const metadata = legacyRow.metadata ?? {};
            const key = buildTxnFingerprint({
                occurredAt: legacyRow.date ?? null,
                amount: legacyRow.amount ?? 0,
                type: legacyRow.type ?? '',
                note: legacyRow.description ?? '',
                persistedCycleTag: typeof metadata.persisted_cycle_tag === 'string' ? metadata.persisted_cycle_tag : null
            });
            const matched = sbFingerprintMap.get(key) ?? [];
            if (matched.length !== 1) {
                failed += 1;
                errors.push(`[cleanup:${legacyRow.id}] ${matched.length === 0 ? 'no-match' : 'ambiguous-match'} for fingerprint`);
                return;
            }
            const source = matched[0];
            const mappedAccountId = accountMap.get(source.account_id) ?? '';
            const mappedToAccountId = source.target_account_id ? accountMap.get(source.target_account_id) ?? '' : '';
            const mappedCategoryId = source.category_id ? categoryMap.get(source.category_id) ?? '' : '';
            const mappedPersonId = source.person_id ? personMap.get(source.person_id) ?? '' : '';
            const mappedShopId = source.shop_id ? shopMap.get(source.shop_id) ?? '' : '';
            const patchBody = {
                account_id: mappedAccountId,
                to_account_id: mappedToAccountId,
                category_id: mappedCategoryId,
                person_id: mappedPersonId,
                shop_id: mappedShopId,
                metadata: {
                    ...metadata,
                    source_id: source.id,
                    cleanup_legacy_at: new Date().toISOString()
                }
            };
            await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$services$2f$pocketbase$2f$server$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["pocketbaseRequest"])(`/api/collections/${PB_TRANSACTIONS_COLLECTION}/records/${legacyRow.id}`, {
                method: 'PATCH',
                body: patchBody
            });
            updated += 1;
        } catch (err) {
            failed += 1;
            errors.push(`[cleanup:${legacyRow.id}] ${String(err)}`);
        }
    });
    return {
        created,
        updated,
        failed,
        errors
    };
}
async function GET(request) {
    const collection = request.nextUrl.searchParams.get('collection') ?? 'all';
    const cleanupLegacy = [
        '1',
        'true',
        'yes'
    ].includes((request.nextUrl.searchParams.get('cleanupLegacy') ?? '').toLowerCase());
    const fresh = [
        '1',
        'true',
        'yes'
    ].includes((request.nextUrl.searchParams.get('fresh') ?? '').toLowerCase());
    const results = {};
    // Always run accounts before transactions so FK relations resolve
    if (collection === 'all' || collection === 'accounts') {
        console.log('[Backfill] Starting accounts...');
        results.accounts = await backfillAccounts();
        console.log('[Backfill] Accounts done:', results.accounts.created, 'created,', results.accounts.updated, 'updated,', results.accounts.failed, 'failed');
    }
    if (collection === 'all' || collection === 'transactions') {
        if (fresh) {
            const purge = await purgeCollectionRecords(PB_TRANSACTIONS_COLLECTION);
            results.transactions_purge = {
                created: 0,
                updated: purge.deleted,
                failed: purge.failed,
                errors: purge.errors
            };
        }
        console.log('[Backfill] Starting transactions...');
        results.transactions = await backfillTransactions();
        console.log('[Backfill] Transactions done:', results.transactions.created, 'created,', results.transactions.updated, 'updated,', results.transactions.failed, 'failed');
        if (cleanupLegacy) {
            console.log('[Backfill] Starting legacy transaction cleanup...');
            results.transactions_cleanup_legacy = await cleanupLegacyTransactions();
            console.log('[Backfill] Legacy cleanup done:', results.transactions_cleanup_legacy.created, 'created,', results.transactions_cleanup_legacy.updated, 'updated,', results.transactions_cleanup_legacy.failed, 'failed');
        }
    }
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f2e$pnpm$2f$next$40$16$2e$0$2e$10_$40$babel$2b$core$40$7$2e$28$2e$5_react$2d$dom$40$19$2e$2$2e$3_react$40$19$2e$2$2e$3_$5f$react$40$19$2e$2$2e$3$2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json(results, {
        status: 200
    });
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__0f3b1f49._.js.map